# CMake package configuration file for the CollisionAlgorithm plugin

@PACKAGE_INIT@

if(NOT TARGET CollisionAlgorithm)
        include("${CMAKE_CURRENT_LIST_DIR}/CollisionAlgorithmTargets.cmake")
endif()

check_required_components(CollisionAlgorithm)

set(CollisionAlgorithm_LIBRARIES "CollisionAlgorithm")
set_and_check(CONSTRAINTGEOMETRYPLUGIN_INCLUDE_DIRS "@COLLISIONALGORITHM_INCLUDE_DIRS@")
